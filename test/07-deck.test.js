const assert = require('chai').assert;
const DeckOfCards = require('../questions/07-deck');

describe('DeckOfCards', () => {
  // Depending on how you construct your deck how do you think it would be best to write tests for it?

  // describe('createDeck()', () => {
  //   it('should be a test', () => {
  //      // this one is a little iffy to write because you might end up constructing your deck differently please write a test that best suits your data structure
  //   });
  // });

  // describe('shuffle()', () => {
  //   it('should be a shuffled deck', () => {
  //
  //   });
  // });

  // describe('dealFive()', () => {
  //   it('should be deal 5 cards', () => {
  //
  //   });
  // });
});
