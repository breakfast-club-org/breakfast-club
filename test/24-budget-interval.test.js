const chai = require('chai');
const assert = chai.assert;
const expect = chai.expect;
const every = require('../questions/24-budget-interval');

describe('Budget Interval', function() {
  it('🤷', function() {
    // up to you!
  });
});
